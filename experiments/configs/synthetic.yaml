# Synthetic data experiment configuration

datasets:
  - synthetic_clustered
  - synthetic_uniform

methods:
  - exact
  - sklearn_kdtree
  - sklearn_balltree
  - lsh
  - annoy
  - hnsw
  - des_knn

k_values:
  - 5
  - 10
  - 20

seeds:
  - 42
  - 123
  - 456

n_queries: 1000

des_knn:
  alpha: 0.01
  window_size: 100
  adaptive_alpha: true

lsh:
  n_tables: 10
  n_bits: 10
  n_probes: 1

annoy:
  n_trees: 10
  search_k: null

hnsw:
  M: 16
  ef_construction: 200
  ef: 10

output:
  dir: results/synthetic
  save_per_query: true
  save_figures: true
